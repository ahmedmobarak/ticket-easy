export interface CardBookingData {seats: number, type: string, payment: string, cardNo: number, pin: number};

export interface WalletBookingData{seats: number, type: string, payment: string, paidAmount: number};